// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: synthify/app/v1/tree_import.proto

package appv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/synthify/backend/internal/gen/synthify/app/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TreeImportServiceName is the fully-qualified name of the TreeImportService service.
	TreeImportServiceName = "synthify.app.v1.TreeImportService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TreeImportServiceImportStifProcedure is the fully-qualified name of the TreeImportService's
	// ImportStif RPC.
	TreeImportServiceImportStifProcedure = "/synthify.app.v1.TreeImportService/ImportStif"
)

// TreeImportServiceClient is a client for the synthify.app.v1.TreeImportService service.
type TreeImportServiceClient interface {
	// ImportStif parses a .stif document and appends its items to the workspace
	// tree. With dry_run set it parses, validates and sanitizes without writing
	// anything — that is what the import preview UI calls. The parser lives on
	// the server so preview and commit can never disagree.
	ImportStif(context.Context, *connect.Request[v1.ImportStifRequest]) (*connect.Response[v1.ImportStifResponse], error)
}

// NewTreeImportServiceClient constructs a client for the synthify.app.v1.TreeImportService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTreeImportServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TreeImportServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	treeImportServiceMethods := v1.File_synthify_app_v1_tree_import_proto.Services().ByName("TreeImportService").Methods()
	return &treeImportServiceClient{
		importStif: connect.NewClient[v1.ImportStifRequest, v1.ImportStifResponse](
			httpClient,
			baseURL+TreeImportServiceImportStifProcedure,
			connect.WithSchema(treeImportServiceMethods.ByName("ImportStif")),
			connect.WithClientOptions(opts...),
		),
	}
}

// treeImportServiceClient implements TreeImportServiceClient.
type treeImportServiceClient struct {
	importStif *connect.Client[v1.ImportStifRequest, v1.ImportStifResponse]
}

// ImportStif calls synthify.app.v1.TreeImportService.ImportStif.
func (c *treeImportServiceClient) ImportStif(ctx context.Context, req *connect.Request[v1.ImportStifRequest]) (*connect.Response[v1.ImportStifResponse], error) {
	return c.importStif.CallUnary(ctx, req)
}

// TreeImportServiceHandler is an implementation of the synthify.app.v1.TreeImportService service.
type TreeImportServiceHandler interface {
	// ImportStif parses a .stif document and appends its items to the workspace
	// tree. With dry_run set it parses, validates and sanitizes without writing
	// anything — that is what the import preview UI calls. The parser lives on
	// the server so preview and commit can never disagree.
	ImportStif(context.Context, *connect.Request[v1.ImportStifRequest]) (*connect.Response[v1.ImportStifResponse], error)
}

// NewTreeImportServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTreeImportServiceHandler(svc TreeImportServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	treeImportServiceMethods := v1.File_synthify_app_v1_tree_import_proto.Services().ByName("TreeImportService").Methods()
	treeImportServiceImportStifHandler := connect.NewUnaryHandler(
		TreeImportServiceImportStifProcedure,
		svc.ImportStif,
		connect.WithSchema(treeImportServiceMethods.ByName("ImportStif")),
		connect.WithHandlerOptions(opts...),
	)
	return "/synthify.app.v1.TreeImportService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TreeImportServiceImportStifProcedure:
			treeImportServiceImportStifHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTreeImportServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTreeImportServiceHandler struct{}

func (UnimplementedTreeImportServiceHandler) ImportStif(context.Context, *connect.Request[v1.ImportStifRequest]) (*connect.Response[v1.ImportStifResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("synthify.app.v1.TreeImportService.ImportStif is not implemented"))
}
