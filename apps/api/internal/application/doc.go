// Package application is the API use-case layer.
//
// Handlers depend on application behavior through consumer-owned interfaces.
// Application services coordinate repositories, domain behavior, transactions,
// notifications, and external ports. Pure business decisions remain in domain.
package application
